# Kora JSON

Kotlin counterpart of `kora-java-json`. It covers generated one-way and two-way
codecs, nullability and inclusion, field naming/skipping, `JsonNullable`, built-in
collections/time types, `RawJson`, enums, sealed/generic types, and field-level
manual `@Mapping` codecs.
