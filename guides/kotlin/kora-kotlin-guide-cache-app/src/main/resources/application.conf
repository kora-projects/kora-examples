httpServer {
  publicApiHttpPort = 8080
  privateApiHttpPort = 8085
  telemetry.logging.enabled = true
}

cache.caffeine.users {
  maximumSize = 1000
  expireAfterWrite = "10m"
}

logging {
  levels {
    "ROOT" = "WARN"
    "io.koraframework" = "INFO"
    "io.koraframework.guide.cache" = "INFO"
  }
}
