soapClient.SimpleService {
  url = ${SOAP_CLIENT_URL}
  timeout = 30s
}

httpClient.default {
  requestTimeout = 10s
  getValuesConfig {
    requestTimeout = 20s
  }
}

logging.level {
  "root": "WARN"
  "ru.tinkoff.kora": "DEBUG"
  "ru.tinkoff.kora.example": "DEBUG"
}
